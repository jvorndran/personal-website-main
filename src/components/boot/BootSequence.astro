---
// Full-screen boot overlay — plays once per session, respects reduced-motion
---

<div
  id="boot-overlay"
  class="fixed inset-0 z-[100] flex flex-col items-center justify-center bg-bg"
  style="display: none;"
  aria-hidden="true"
>
  <div class="mb-8 text-xs uppercase tracking-[0.3em] text-muted/40">
    jvorndran.github.io/personal-website-main
  </div>
  <div
    id="boot-terminal"
    class="w-full max-w-md px-6 font-mono text-sm leading-loose text-muted"
  />
  <div class="boot-cursor mt-6 h-4 w-2 bg-accent-hero" />
</div>

<style>
  .boot-line {
    opacity: 0;
    animation: boot-type 0.3s ease-out forwards;
  }

  @keyframes boot-type {
    from {
      opacity: 0;
      transform: translateX(-8px);
    }
    to {
      opacity: 1;
      transform: translateX(0);
    }
  }

  .boot-cursor {
    animation: blink-cursor 0.6s step-end infinite;
    transition: opacity 0.3s ease-out;
  }

  @media (prefers-reduced-motion: reduce) {
    #boot-overlay {
      display: none !important;
    }
  }
</style>

<script>
  import { initBootSequence } from '../../scripts/boot-sequence';
  initBootSequence();
</script>
